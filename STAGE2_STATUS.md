# 2단계 실행 안정화 결과

## 실행

Node.js 22 이상 필요. 이 폴더에서:

- npm start: 127.0.0.1:3000에서 기존 화면 미리보기 (PORT로 변경 가능)
- npm test: 네트워크·비밀값·학생 데이터 쓰기 없는 격리 회귀 테스트
- GET /api/health: 실제 연결과 설정 여부를 구분한 상태

.env.local이 .env보다 우선이며, 이미 설정된 프로세스 환경변수는 유지한다. .env.example은 비밀값 없는 템플릿이다. 기존 .env는 수정하지 않았다.

## 변경

- 공개 HTML/에셋 허용목록 및 실제 경로/심볼릭 링크 검사. env, Git, 데이터, 서버 소스 제공 차단.
- 잘못 인코딩된 URL은 400. 공개 파일의 HEAD 지원과 nosniff 응답.
- 실제 인증/담당 학급 검증이 없는 보호 API는 503 AUTH_NOT_CONFIGURED로 차단. 이는 완성된 로그인 구현이 아니다.
- CORS 전체 출처 허용 제거.
- Evidence JSON 오류 수정, 카드 형식 검사, 원문 미검증 상태 명시.
- 예시 음성 자동 제출/가상 상대 발언/허위 세특 fallback 제거.
- Sheets 미연결 시 성공으로 표시하지 않고 기존 큐 유지.
- 발언 HTML 문자열의 사용자 입력 escape.
- 각 학생 화면에 예시 데이터 포함 및 실제 인증 연결 전 안내. 기존 디자인 전체 개편은 하지 않음.
- Gemini 모델 추측 기본값 제거. 미설정 시 명시적 오류. API 오류 응답 본문을 로그 오류에 포함하지 않음.

## 검증

npm test: 10개 통과. 비공개 경로 14종 차단, 보호 API GET/POST 차단, 공개 HTML/HEAD, 잘못된 URL, 상태 API, 카드 로드, 빈 자격증명 예제, 큐 보존, 세특 차단, HTML 인라인 JS 문법 포함.

원본 store.json/syncQueue.json과 .env는 수정하지 않았다. 실제 Firebase/Gemini/Sheets 연결 성공으로 판정하지 않았다. Gemini 모델 목록 인증 요청은 자동 승인 검토에서 명시적 키 전송 승인 부재로 거절되어 실행되지 않았다.

브라우저 390px 화면/키보드 QA, 실제 서버 네트워크 실행, 실모델 호출은 아직 미검증이다. 기존 Phase 테스트는 데모 인증과 쓰기를 전제하므로 현재 안전한 미리보기 모드의 합격 기준으로 사용하지 않는다.

## 후속

1. 승인 후 Gemini 공식 API의 모델 목록 접근부터 확인. 학생 데이터를 보내는 생성 요청은 하지 않는다.
2. 실제 Firebase 인증 및 학생/교사·학급 권한을 구현한 뒤 보호 API를 검증된 경로별로 개방.
3. 기존 기록의 테스트 데이터 여부를 분리하고 고정 점수/예시 분석을 실제 근거 기반 결과로 교체.
4. 디자인 단계에서 Cyber Neon Arena 공통 토큰과 390px 화면 QA 수행.
5. 기존 배포의 '앱 구성 오류'는 발생 URL/로그가 없어 원인 미확정. 로컬 Evidence 오류와 동일 원인으로 단정하지 않음.

