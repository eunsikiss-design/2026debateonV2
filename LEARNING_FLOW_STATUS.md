# 주제 중심 학습 흐름 — 2026-09-24

## 사용 방법

- 학생 로그인 완료 → **주제에서 시작하는 나의 생각** → 단원·주제 선택 → 개념을 눌러 뜻 확인 → 기초 논리 연습 / 심화 논술 / 스피치 선택.
- 기초 논리 연습: 논제 아래 **만약에** 이야기를 읽고 주장과 이유를 쓴다. 개념 도움말과 일반적인 관찰 기준은 필요할 때 펼친다. ‘다르게 생각해 보기’는 선택 사항이다.
- 교사 관리자 → **핵심 단어**: 단어·쉬운 뜻풀이·적용 주제·출처 메모를 추가/수정/삭제한다. 다음 피드백 요청부터 서버가 최신 목록을 읽는다. 학생 화면의 도움말은 주제 재선택 또는 새로고침 시 갱신한다. 과거 피드백은 변경하지 않는다.
- 교사 관리자 → **40논제 수업 자료**: 40개 루브릭, A~E 예시답안 200개, 찬반 3수준 입론 240개, 예상 문답 80개를 열람한다. 성취수준 연결은 참고 판단이며 최종 성적이 아니다.

## 자료·피드백 연결

`data/learning-materials.json`은 기존 자료집을 앱 데이터로 옮긴 작성 자료이다. 교과 핵심 단어는 보존하고 어려운 비핵심 표현을 쉬운 표현으로 정리했다. 논제마다 서로 다른 이해관계와 조건을 추가한 가상 상황이 있다. 교과서 PDF나 학생 명단은 포함하지 않는다.

학생 API에는 논제·상황·개념·기초 관찰 기준만 반환한다. 교사 예시답안은 교사 전용 API로 제공하며 정적 파일 접근은 차단한다. Gemini 요청에는 선택한 논제의 자료만 포함한다. 기초는 일반적인 관찰 기준과 예상 질문, 심화는 루브릭·예시답안, 스피치는 찬반 입론·예상 문답을 참고한다. 예시를 복사하거나 정해진 찬반 결론으로 유도하지 않도록 지시한다.

교사의 단어 편집은 학교 단위로 분리하며 버전 충돌을 확인한 뒤 파일을 원자적으로 교체한다. 기본 저장 위치는 `DATA_STORE_PATH`와 같은 폴더의 `teaching-config.json`이다. Railway에서 기존 데이터 저장 경로가 영구 볼륨을 가리키면 이 파일도 같은 볼륨에 유지된다. 필요하면 `TEACHING_CONFIG_PATH`로 별도 지정한다. 실행 중 수정 파일은 Git에서 제외한다.

원문 발췌가 없는 논제도 작성된 수업 상황과 개념 정의로 논리 코칭이 가능하다. 가상 이야기를 실제 사실·통계로 인용하지 않는다. 원문 인용이 있는 응답은 기존 발췌 대조 검사를 거친다. 실제 응답이 실패하면 기본 도움말이라고 명확히 표시한다. 글 길이·개념 이름 개수만으로 우수 평가를 만들지 않는다.

## 검증

- `npm run test:learning`: **29/29 통과**. 40개 자료 완전성, 98개 초기 단어, 교사 편집/삭제/학교 분리/재시작 후 유지/버전 충돌, 학생 접근 차단, 서버 논제 변조 방지, 세 활동의 Gemini 요청 내용 및 실패 대체 도움말 검증.
- Chrome 실제 브라우저 + 로컬 HTTP 서버 + 테스트 전용 인증: 주제·개념·활동 선택, 기초 피드백→수정→저장, 심화 글 제출, 스피치 이동, 390px 화면 넘침 없음, 교사 단어 저장→학생 화면 갱신, 교사 전체 자료 열람 통과. 실제 학생 계정과 외부 Gemini 호출은 사용하지 않았다.
- 기존 전체 검사: 변경 전 기록과 동일한 근거 자료 관련 **11개 실패**가 남아 있다. 로컬의 비공개 원문 corpus 부재와 기존 근거 API 테스트의 인증 fixture 불일치에 관한 실패이며 이 변경의 기능 검사와 구분한다.
- 실제 Gemini 내용의 교육적 적절성·정확성은 운영 수업에서 교사가 계속 확인해야 한다. 이번에는 실제 응답의 정확성이 검증되었다고 표시하지 않았다.

## 재생성

기존 로컬 자료집이 있을 때만 `node scripts/build-learning-materials.cjs`로 초기 작성 자료와 기본 단어 데이터를 재생성할 수 있다. 운영 중 교사가 편집한 설정 파일은 덮어쓰지 않는다. 배포에는 생성된 JSON을 포함하므로 원본 자료집 폴더가 필요 없다.
