(() => {
  const node=(tag,text,cls)=>{const n=document.createElement(tag);if(text!==undefined)n.textContent=text;if(cls)n.className=cls;return n;};
  function concepts(words){const box=node('div',undefined,'concept-list');if(!words?.length)box.append(node('p','이 주제에 등록된 핵심 단어가 없습니다. 자신의 말로 이유를 설명해도 좋아요.'));for(const w of words||[]){const item=node('details');item.append(node('summary',w.term),node('p',w.definition));box.append(item);}return box;}
  function scenario(s,advanced=false){const box=node('section',undefined,'what-if');box.setAttribute('aria-label',s.title||'만약에');box.append(node('h3',s.title||'만약에'),node('p',s.story));if(s.complication)box.append(node('p',s.complication));if(advanced&&s.advancedComplication)box.append(node('p',s.advancedComplication));if(s.perspectives){box.append(node('p','핵심 쟁점: '+s.issue,'scenario-issue'));const positions=node('div',undefined,'issue-positions');for(const p of s.perspectives){const card=node('section',undefined,'issue-position');card.append(node('h4',p.label),node('p',p.reason));positions.append(card);}box.append(positions);}if(s.choiceQuestion)box.append(node('p',s.choiceQuestion,'scenario-question'));else if(s.invitation)box.append(node('p',s.invitation,'scenario-invitation'));return box;}
  function details(title,content){const d=node('details',undefined,'learning-details');d.append(node('summary',title),content);return d;}
  function rubric(items){const list=node('ul');for(const r of items)list.append(node('li',r.name+' — '+r.description));return list;}
  function activityCards(lesson){const grid=node('div',undefined,'activity-grid');for(const a of lesson.activities){const link=node('a',undefined,'activity-card');link.href=a.href+'?topic='+encodeURIComponent(lesson.topic.topicId);const icon=node('span',a.icon,'activity-icon');icon.setAttribute('aria-hidden','true');const heading=node('div',undefined,'activity-heading');heading.append(icon,node('h3',a.title));link.append(heading,node('p',a.description),node('strong','소크라AI와 이렇게 해요'),node('p',a.plan,'activity-plan'),node('span','시작하기 →','activity-start'));grid.append(link);}return grid;}
  window.LearningUI={node,concepts,scenario,details,rubric,activityCards};
})();
