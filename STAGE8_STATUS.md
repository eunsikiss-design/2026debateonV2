# 교과 근거 자료실 — 2026-09-22

## 구현 범위
- 제공된 PDF 3개(88쪽)와 HWP 6개에서 94개 페이지/구역, 254개 원문 발췌를 생성했습니다.
- 학생 공개 자료는 교과서·용어 사전 2개(81개 발췌)입니다. 지도서·개념 확인 문제 7개는 인증된 교사만 검색·열람합니다. 원문 파일 열람은 로그인이 필요합니다.
- 카드에 원문 파일명, SHA-256, 자료 ID, 발췌 ID, PDF 파일 쪽수 또는 HWP 구역, 텍스트 오프셋을 기록합니다. 모든 발췌는 추출 본문의 연속 구간입니다.
- `/stitch_screens/11_evidence_library.html`에서 키워드 검색, 자료 선택, 출처 복사, 원문 연결을 제공합니다.
- 기초 연습 Gemini 요청에 학생용 검색 결과 최대 4개를 넣고 서버에서 확정한 참조 ID를 반환합니다. 화면에서 참조 자료를 열 수 있습니다. 참조는 모델에 제공한 자료 목록이며 문장별 인용 검증 결과는 아닙니다.
- 기존 `data/evidence_cards.json`은 이번 서비스에서 사용하지 않습니다.

## 재추출
`python -m pip install -r scripts/requirements-knowledge.txt` 후 `npm run knowledge:ingest`를 실행하고 서버를 재시작합니다. `PYTHON` 환경 변수로 Python 실행 파일을 지정할 수 있습니다. 로컬 추출이며 API 키와 외부 업로드가 필요 없습니다. 실패 시 기존 corpus를 덮어쓰지 않습니다.

## 검증과 남은 범위
- 자동 검증: `npm test`에 출처 일치, 학생/교사 권한, 원문 변경 감지, 검색, API, Gemini 프롬프트 연결 테스트를 포함합니다. Gemini 테스트는 네트워크 호출을 대체합니다.
- 원문 품질: 교과서 PDF 9쪽을 렌더링해 텍스트와 대조했습니다. 전체 페이지의 시각 검수는 완료하지 않았습니다.
- 검색 방식은 한국어 공백 차이를 허용하는 로컬 키워드 검색입니다. 의미 임베딩·벡터 DB·재순위화는 아직 구현하지 않았습니다.
- 모든 자료는 교사 검토 전입니다. 표·그림·수식, HWP 읽기 순서, 세부 목차는 원문 대조가 필요합니다. HWP 쪽수와 PDF 인쇄 쪽수를 추정하지 않습니다. 교사 승인 편집 UI는 아직 없습니다.
- 파일 변경 시 원문 열람을 차단하고, 서버 재시작 시 해당 자료를 검색에서 제외합니다. 원본을 바꾼 후 반드시 재추출·재시작해야 합니다.
- 실제 Google 로그인 세션의 학생/교사 왕복 검증과 실제 Gemini 응답의 근거 충실도 검수는 별도 필요합니다. 저장 계층은 기존 로컬 JSON이며 Firestore 이전은 이번 범위에 포함되지 않습니다.

## 최종 검증 결과
- 자동 테스트 38개 통과. 브라우저에서 검색 성공·빈 결과·출처 선택 동작을 확인했습니다.
- PDF 두 단 문장의 혼합을 발견해 콘텐츠 순서 기반 추출로 수정하고 회귀 테스트를 추가했습니다. PDF의 세부 장 제목은 검토 전까지 부여하지 않습니다.

