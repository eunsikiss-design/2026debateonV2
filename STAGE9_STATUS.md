# 원문 검수·의미 검색·근거 검증

## 교사 검수
`/stitch_screens/12_evidence_review.html`에서 인증된 교사가 원문을 열고 승인·보류·검수 전 상태를 저장합니다. 승인에는 원문 대조 확인이 필요합니다. 보류에는 사유가 필요하며 학생 검색과 AI 참고자료에서 제외됩니다. 검수자 UID는 서버 인증 정보에서만 가져오며 변경 이력을 보관합니다. 동시에 저장하면 이전 버전은 거절합니다. 발췌 내용이 바뀌면 기존 승인은 적용되지 않습니다.

현재 사람이 검수 승인한 자료는 없습니다. 이 작업에서 교사 승인을 대신 생성하지 않았습니다. 공동 교과 자료에 대한 검수 상태는 앱 전체에서 공유됩니다.

## 로컬 의미 검색
Xenova/multilingual-e5-small의 고정 리비전 모델을 다운로드해 로컬에서 실행합니다. 원문과 검색어는 임베딩 외부 API로 전송하지 않습니다. 검색어는 query, 원문은 passage 접두어로 구분합니다. 긴 발췌는 180자 단위로 나누고 가장 유사한 구간과 전체 평균의 유사도를 함께 사용합니다. 의미 유사도와 키워드 점수를 결합하며 자료 권한·보류 상태를 먼저 적용합니다. 인덱스 불일치나 모델 오류 시 키워드 검색으로 전환하고 화면에 알립니다.

재현: `npm install`, `npm run knowledge:model`(공개 모델 다운로드), `npm run knowledge:embed`(로컬 원문 변환), 서버 재시작. 모델은 data/embedding-model, 인덱스는 data/curriculum_embeddings.json에 저장되며 정적으로 공개되지 않습니다.

모델 문서: https://huggingface.co/Xenova/multilingual-e5-small

## Gemini 근거 검사
기초 코칭 응답에 사용한 자료 ID·직접 인용·연결 설명을 요청합니다. 서버가 실제 제공 자료 ID와 원문 인용 문자열을 검사하고, 불일치 시 기존 로컬 피드백으로 전환합니다. `quote_matched`는 인용 일치만 뜻하며 의미 해석의 정확성을 보증하지 않습니다. 사용자의 원문 전송 승인 후 실제 교과 자료로 실험 보고서를 생성했습니다.

실제 검증 준비: `npm run knowledge:verify-live -- --allow-source-transfer`. 롤스·사회 보험의 오개념, 필요에 따른 분배, 교과 밖 질문 네 사례의 원문 발췌와 가상 학생 답변을 Google Gemini에 전송합니다. 실제 학생 개인정보는 포함하지 않습니다. 최초 자동 승인 검토에서 차단되었으나, 이후 사용자가 전송을 명시적으로 승인하여 실행했습니다. 결과는 reports/gemini-grounding-live.json에 저장하고 사람이 설명과 원문을 대조해야 합니다.

## 검증
자동 테스트 45개가 모두 통과했습니다. 검수 권한·버전 충돌·재시작 후 보존·보류 제외, 의미 검색 필터·장애 시 전환, 허위 ID·허위 인용 차단을 포함합니다.

서버 재시작 후 실제 API에서 local-semantic-hybrid 응답을 확인했습니다. 브라우저에서 ‘아프거나 직장을 잃었을 때 함께 비용을 부담하는 제도’를 검색했을 때 사회 보험을 설명하는 교과서 PDF 21쪽이 첫 결과로 표시됐습니다. 로그인하지 않은 교사 검수 화면은 로그인을 요구합니다. 실제 교사 계정의 저장 화면 흐름과 사람의 원문 검수는 아직 완료하지 않았습니다. 실제 Gemini 응답 검증 결과는 아래와 같습니다.

## 실제 Gemini 검증 결과
사용자 승인 후 교과서·용어 사전 발췌와 가상 답변을 전송했습니다. 실제 학생 개인정보는 사용하지 않았습니다. 4개 사례 중 3개에서 응답을 받았습니다. 롤스·사회 보험 사례는 인용이 원문과 일치하며 오개념을 교정했습니다. 교과 밖 질문은 근거 부재를 명시하고 인용을 만들지 않았습니다. 필요에 따른 분배 사례는 첫 실패 뒤 두 번 재시도했으며 재시도 모두 HTTP 503으로 내용 검증을 완료하지 못했습니다.

추가 발견: 응답 JSON 항목이 앱의 BASIC_WRITING_JSON_SCHEMA와 일치하지 않아 구조화 응답 강제 및 표시·채점 호환성 검증이 필요합니다. 교과 밖 질문의 긴 검색문에 포함된 일반 단어 때문에 무관한 자료가 검색되는 문제도 남아 있습니다. 이 결과는 에이전트의 발췌 대조이며 교사의 원문 검수 승인은 아닙니다. 보고서: reports/gemini-grounding-live.json.

